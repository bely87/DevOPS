Server={%- for item in zabbix_proxy_list -%}
{{ item }}
{%- if not loop.last -%},{%- endif -%}
{%- endfor %}

ServerActive={%- for item in zabbix_proxy_list -%}
{{ item }}
{%- if not loop.last -%},{%- endif -%}
{%- endfor %}
